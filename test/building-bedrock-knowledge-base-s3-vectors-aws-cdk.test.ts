// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as BuildingBedrockKnowledgeBaseS3VectorsAwsCdk from '../lib/building-bedrock-knowledge-base-s3-vectors-aws-cdk-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/building-bedrock-knowledge-base-s3-vectors-aws-cdk-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new BuildingBedrockKnowledgeBaseS3VectorsAwsCdk.BuildingBedrockKnowledgeBaseS3VectorsAwsCdkStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
